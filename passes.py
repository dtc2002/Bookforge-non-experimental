def generate_book_outline(title, author, genre, ollama_model="llama3"):    """Generate a detailed book outline using Ollama."""    prompt = f""")    Outline a full-length literary work titled '{title}' by {author} in the {genre} genre.    Structure:    1. Acts/Parts (3-5 parts)    2. Chapter lists per book    3. Arc mapping (character development)    4. Cliffhangers at chapter endings    Use {ollama_model} for content generation."""    response = ollama.generate(model=ollama_model, prompt=prompt)    return json.loads(response['response'])